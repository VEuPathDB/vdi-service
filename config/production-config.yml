$schema: https://veupathdb.github.io/vdi-service/schema/stack-config.json

definitions:
  genomicsProjects: &genomicsProjects
  - AmoebaDB
  - CryptoDB
  - FungiDB
  - GiardiaDB
  - HostDB
  - MicrosporidiaDB
  - PiroplasmaDB
  - PlasmoDB
  - ToxoDB
  - TrichDB
  - TriTrypDB
  - VectorBase
  - VEuPathDB
  vdiSchemaPass: &vdiSchemaPass ${env:VDI_APPDB_SCHEMA_PASS}
  vdiControlSchema: &vdiControlSchema vdi_control_prod_${env:LOCATION_INDICATOR}
  vdiDataSchema: &vdiDataSchema vdi_datasets_prod_${env:LOCATION_INDICATOR}
  datasetPropertiesJsonSchemaFiles:
  - &genDatasetProps /schema/data/dataset-characteristics.genomics.json
  - &edaDatasetProps /schema/data/dataset-characteristics.eda.json

containerCore:
  authentication:
    oauth:
      clientId: ${env:OAUTH_CLIENT_ID}
      clientSecret: ${env:OAUTH_CLIENT_SECRET}
      url: https://eupathdb.org/oauth
      keystoreFile: ${env:KEY_STORE_FILE}
      keystorePass: ${env:KEY_STORE_PASS_PHRASE}
    adminToken: ${env:ADMIN_AUTH_TOKEN}

vdi:
  ldap:
    servers: ${env:LDAP_SERVER}
    baseDn: ${env:DB_LOOKUP_BASE_DN}
  siteBuild: ${env:SITE_BUILD}
  objectStore:
    bucketName: vdi-prod
    host: ${env:S3_HOST}
    accessToken: ${env:S3_ACCESS_TOKEN}
    secretKey: ${env:S3_SECRET_KEY}
  rabbit:
    global:
      connection:
        host: ${env:GLOBAL_RABBIT_HOST}
        user: ${env:GLOBAL_RABBIT_USERNAME}
        pass: ${env:GLOBAL_RABBIT_PASSWORD}
      exchange:
        name: vdi-bucket-notifications
      queue:
        name: vdi-bucket-notifications
      routing:
        key: vdi-bucket-notifications
  kafka:
    servers:
    - kafka:9092
    listeners:
    - PLAINTEXT://kafka:9092
  cacheDb:
    user: vdi
    pass: ${env:CACHE_DB_PASSWORD}
    host: cache-db:5432
  plugins:
  - server: bigwig:80
    displayName: bigWig
    dataTypes:
    - name: bigwigfiles
      version: 1.0
    projectIds: *genomicsProjects
  - server: biom:80
    displayName: BIOM
    dataTypes:
    - name: biom
      version: 1.0
    projectIds:
    - MicrobiomeDB
  - server: genelist:80
    displayName: Gene List
    dataTypes:
    - name: lightweight
      version: 1.0
    projectIds: *genomicsProjects
  - server: isasimple:80
    displayName: ISA Study
    dataTypes:
    - name: isasimple
      version: 1.0
    projectIds:
    - ClinEpiDB
  - server: noop:80
    displayName: Lightweight
    dataTypes:
    - name: lightweight
      version: 1.0
  - server: phenotype:80
    displayName: Phenotype
    dataTypes:
    - name: phenotype
      version: 1.0
    projectIds: *genomicsProjects
  - server: rnaseq:80
    displayName: RNA-Seq
    dataTypes:
    - name: rnaseq
      version: 1.0
    projectIds: *genomicsProjects

  installTargets:
  - targetName: AmoebaDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_AMOEBA}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_AMOEBA}
    propertySchema: *genDatasetProps

  - targetName: ClinEpiDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_CLINEPI}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_CLINEPI}
    propertySchema: *edaDatasetProps

  - targetName: CryptoDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_CRYPTO}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_CRYPTO}
    propertySchema: *genDatasetProps

  - targetName: FungiDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_FUNGI}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_FUNGI}
    propertySchema: *genDatasetProps

  - targetName: GiardiaDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_GIARDIA}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_GIARDIA}
    propertySchema: *genDatasetProps

  - targetName: HostDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_HOST}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_HOST}
    propertySchema: *genDatasetProps

  - targetName: MicrobiomeDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_MICROBIOME}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_MICROBIOME}
    propertySchema: *edaDatasetProps

  - targetName: MicrosporidiaDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_MICROSPORIDIA}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_MICROSPORIDIA}
    propertySchema: *genDatasetProps

  - targetName: OrthoMCL
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_ORTHO}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_ORTHO}
    propertySchema: ~

  - targetName: PiroplasmaDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_PIROPLASMA}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_PIROPLASMA}
    propertySchema: *genDatasetProps

  - targetName: PlasmoDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_PLASMO}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_PLASMO}
    propertySchema: *genDatasetProps

  - targetName: ToxoDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_TOXO}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_TOXO}
    propertySchema: *genDatasetProps

  - targetName: TrichDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_TRICH}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_TRICH}
    propertySchema: *genDatasetProps

  - targetName: TriTrypDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_TRITRYP}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_TRITRYP}
    propertySchema: *genDatasetProps

  - targetName: VectorBase
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_VECTOR}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_VECTOR}
    propertySchema: *genDatasetProps

  - targetName: VEuPathDB
    controlDb:
      user: *vdiControlSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_UNIDB}
    dataDb:
      user: *vdiDataSchema
      pass: *vdiSchemaPass
      ldapCn: ${env:LDAP_UNIDB}
    propertySchema: ~

