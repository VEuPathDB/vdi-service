package vdi.module.events.routing

import vdi.component.modules.VDIServiceModule

/**
 * Represents an instance of the `event-router` VDI Service module.
 */
sealed interface EventRouter : VDIServiceModule
