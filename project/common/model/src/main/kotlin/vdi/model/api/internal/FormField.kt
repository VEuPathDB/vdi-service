package vdi.model.api.internal

/**
 * Multipart form field names used in communication between VDI stack services.
 */
object FormField {
  const val Details  = "details"
  const val Manifest = "manifest"
  const val Metadata = "meta"
  const val Payload  = "payload"
}
